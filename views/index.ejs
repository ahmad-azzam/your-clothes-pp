<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;800&display=swap" rel="stylesheet">
    <title>
        yourClothes
    </title>
    <script src="https://kit.fontawesome.com/fb9d24a49a.js" crossorigin="anonymous"></script>
    <style>
        :root {
            --red: #f9032f;
            --black: #000;
            --white: #fff;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
        }

        .hero {
            background: linear-gradient(to right, rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.9)), url('https://static.highsnobiety.com/thumbor/8uQ9HZ_XpAo-rQErI9vvUVCcp6s=/1600x1067/static.highsnobiety.com/wp-content/uploads/2021/04/16162418/how-to-care-for-clothes-02.jpg');
            height: 100vh;
            background-size: cover;
            background-position: center;
            padding: 0.5rem calc((100vw - 1200px)/2);
            display: flex;
            justify-content: flex-start;
            align-items: center;
        }

        .hero__content {
            color: var(--white);
            padding: 3rem 1rem;
            line-height: 1;
        }

        .hero__content h1 {
            font-size: clamp(3rem, 8vw, 7rem);
            text-transform: uppercase;
            margin-bottom: 0.5rem;
        }

        .hero__content p {
            font-size: clamp(1rem, 4vw, 2.5rem);
            margin-bottom: 3rem;
        }

        .button-hero {
            padding: 1rem 3rem;
            border: none;
            background: var(--red);
            color: var(--white);
            font-size: 1rem;
            cursor: pointer;
            outline: none;
            transition: 0.3s ease;
            text-decoration: none;
            border-radius: 4px;
        }

        .button-hero:hover {
            background: var(--black);
        }
    </style>
</head>

<body>
    <div class="hero">
        <div class="hero__content">
            <h1 class="animate-hero">YOUR CLOTHES</h1>
            <p class="animate-hero">All your clothes needs are here </p>
            <a href="/login" class="button-hero animate-hero">LOGIN NOW</a>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.7.1/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.7.1/ScrollTrigger.min.js"></script>
    <script src="/js/index.js"></script>
</body>

</html>